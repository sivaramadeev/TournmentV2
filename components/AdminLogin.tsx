
// This component has been replaced by AuthPage.tsx to support SaaS features (Login/Register).
// Keeping file to prevent import errors during transition if cached.
export default function AdminLogin() { return null; }
